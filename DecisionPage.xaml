<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Calculator.DecisionPage"
             Title="DecisionPage" x:Name="Toolbar">

    <Grid x:Name="grid">
        <VerticalStackLayout HorizontalOptions="Center" Margin="15,30" Spacing="15" x:Name="stack"/>
        <Button Text="Решить" VerticalOptions="End" Margin="30" CornerRadius="10" x:Name="button" Clicked="button_Clicked"
                FontFamily="medium"/>
    </Grid>

</ContentPage>